from nlp.router import app
'''pip freeze : used to Output installed packages in requirements format.
packages are listed in a case-insensitive sorted order.\
pip-chill : Like pip freeze but lists only the packages that are not dependencies of installed packages.'''

